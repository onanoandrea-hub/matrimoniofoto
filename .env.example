NEXT_PUBLIC_API_BASE_URL=http://fotoonano.duckdns.org

# Campo multipart = nome in multer (default files). Es: NEXT_PUBLIC_UPLOAD_FIELD=photos

# Stesso valore di TOKEN nel .env del server Express sul Raspberry (solo server-side)
UPLOAD_API_KEY=VALORE_DEL_TOKEN_RASPBERRY

